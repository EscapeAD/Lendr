<%= form_for message do |f| %>

  <%= f.hidden_field :verification_id, value: verification_id.id %>

  <%= f.label :content, "Enter Message" %>
  <%= f.text_field :content, autocomplete: :off, id: 'enterChat', class: 'theClear', placeholder: "Enter text here" %>

<% end %>
