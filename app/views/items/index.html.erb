<h1>All Items</h1>

<% @items.each do |item| %>
  <div class = "item_image"><%= image_tag item.image.url %></div>
  <div class = "item-info">
    <%= link_to item.name, item_path(item) %>
    <br>Owner: <%= item.user_id %>
  </div>
  <br>
<% end %>
</ul>
