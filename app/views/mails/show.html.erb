<% user = User.find_by(id: @mail.sender) %>

<div class='container'>
<div class="page-header">
  <h1><%=current_user.first_name.capitalize%> <%=current_user.last_name.capitalize%>'s <small>Inbox</small></h1>
</div>

<div class='container'>
  <div class="row">
    <div class="col-md-2">
      <h4>About</h4>
      <Strong>Sender:</strong> <%= User.find(@mail.sender).first_name %> <%= User.find(@mail.sender).last_name %><br/>
  </div>
  <div class="col-md-8 mailbox">
    <h4>Title: <small><%= @mail.title %></small></h4>
      <hr>
    <p><%= @mail.text %></p>


  </div>
  </div>
</div>
</div>
</div>
