<% user = User.find_by(id: @mail.sender) %>
<%= user.first_name %>
</br>
<%= @mail.title %>
</br>
<%= @mail.text %>
<div class='container'>
<div class="page-header">
  <h1><%=current_user.first_name.capitalize%> <%=current_user.last_name.capitalize%>'s <small>Inbox</small></h1>
</div>

<div class='container'>
  <div class="row">
    <div class="col-md-2"><h4>About</h4>
      <Strong>Sender:</stong> <%= User.find(@mail.sender).first_name %> <%= User.find(@mail.sender).last_name %><br/>
  </div>
  <div class="col-md-8">
    <h4><%= @mail.title %></h4>
    <p><%= @mail.text %></p>


  </div>
  </div>
</div>
</div>
</div>
